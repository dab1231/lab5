# 🎓 Лабораторная работа №5 - Рефлексия в Java

## 📝 Задание

Написать код, который с помощью **рефлексии** вызывает метод для модификации значения цены модели по её названию класса Автомобиль.

В параметрах командной строки приложения указывается:
- Полное имя класса
- Имя метода (нестатический)
- Параметры для этого метода

На экран выводится результат выполнения метода (информация об автомобиле).

---

## ✅ Решение

Созданы два класса для выполнения задания:

### 1. `ReflectionTask.java` - Базовая реализация
Простая и понятная реализация задания с детальными комментариями.

**Возможности:**
- ✅ Загрузка класса по имени через `Class.forName()`
- ✅ Создание экземпляра через рефлексию
- ✅ Получение и вызов метода `setModelPrice`
- ✅ Вывод состояния до и после изменения
- ✅ Полная обработка ошибок
- ✅ Дополнительная информация о классе

### 2. `ReflectionDemo.java` - Расширенная демонстрация
Улучшенная версия с множеством примеров и красивым форматированием.

**Возможности:**
- ✅ Демонстрация работы с классом `Auto`
- ✅ Демонстрация работы с классом `Motorcycle`
- ✅ Анализ структуры классов через рефлексию
- ✅ Поддержка параметров командной строки
- ✅ Красиво форматированный вывод с эмодзи

---

## 🚀 Быстрый старт

### Вариант 1: Использование bat-файлов (Windows)

```bash
# Компиляция и запуск демонстрации
run.bat

# Запуск конкретной задачи
run_task.bat Vehicle.Auto setModelPrice BMW0 5500000
```

### Вариант 2: Ручная компиляция

```bash
# Компиляция
javac -d bin -encoding UTF-8 src\Vehicle\*.java src\Exceptions\*.java src\ReflectionTask.java src\ReflectionDemo.java

# Запуск ReflectionTask
java -cp bin ReflectionTask Vehicle.Auto setModelPrice BMW0 5500000

# Запуск ReflectionDemo
java -cp bin ReflectionDemo
```

---

## 📚 Примеры использования

### Пример 1: Изменение цены модели Auto

```bash
java -cp bin ReflectionTask Vehicle.Auto setModelPrice BMW0 5500000
```

**Вывод:**
```
=== Загружен класс: Vehicle.Auto ===

=== Информация об автомобиле ДО изменения цены ===
Марка: TestMark
Модели:
Название: BMW0, Цена: 1567234.0
Название: BMW10, Цена: 2843567.0
Название: BMW20, Цена: 3912345.0
---------------------------------------------

Найден метод: setModelPrice
Параметры метода: [class java.lang.String, double]

Вызываем метод: setModelPrice("BMW0", 5500000.0)
Метод успешно выполнен!

=== Информация об автомобиле ПОСЛЕ изменения цены ===
Марка: TestMark
Модели:
Название: BMW0, Цена: 5500000.0  ← ИЗМЕНЕНО!
Название: BMW10, Цена: 2843567.0
Название: BMW20, Цена: 3912345.0

=== Дополнительная информация о классе ===
Реализованные интерфейсы: 
  - Vehicle

Доступные методы класса:
  - setModelPrice([class java.lang.String, double])
  - getModelPrice([class java.lang.String])
  - addNewModel([class java.lang.String, double])
  - deleteModel([class java.lang.String])
  ...
```

### Пример 2: Работа с Motorcycle

```bash
java -cp bin ReflectionTask Vehicle.Motorcycle setModelPrice BMW10 6000000
```

### Пример 3: Расширенная демонстрация

```bash
java -cp bin ReflectionDemo
```

Покажет:
- 🚗 Работу с классом Auto
- 🏍️ Работу с классом Motorcycle
- 🔍 Анализ структуры классов
- 💡 Подсказки по использованию

---

## 🎯 Технические детали реализации

### Использованные методы Reflection API

```java
// 1. Получение класса по имени
Class<?> vehicleClass = Class.forName("Vehicle.Auto");

// 2. Получение конструктора
Constructor<?> constructor = vehicleClass.getConstructor(String.class, int.class);

// 3. Создание экземпляра
Vehicle vehicle = (Vehicle) constructor.newInstance("TestMark", 3);

// 4. Получение метода
Method method = vehicleClass.getMethod("setModelPrice", String.class, double.class);

// 5. Вызов метода
method.invoke(vehicle, "BMW0", 5500000.0);

// 6. Получение интерфейсов
Class<?>[] interfaces = vehicleClass.getInterfaces();

// 7. Получение всех методов
Method[] methods = vehicleClass.getDeclaredMethods();
```

### Обработка исключений

```java
try {
    // Код с рефлексией
} catch (ClassNotFoundException e) {
    // Класс не найден
} catch (NoSuchMethodException e) {
    // Метод не найден
} catch (InvocationTargetException e) {
    // Ошибка при выполнении метода
    // e.getCause() - реальное исключение
} catch (InstantiationException | IllegalAccessException e) {
    // Ошибки при создании объекта
}
```

---

## ⚠️ Валидация и ограничения

### Допустимые значения

| Параметр | Ограничение |
|----------|-------------|
| Класс | `Vehicle.Auto` или `Vehicle.Motorcycle` |
| Метод | `setModelPrice` |
| Модель | `BMW0`, `BMW10`, `BMW20` (созданные автоматически) |
| Цена | От 1,000,000 до 10,000,000 |

### Примеры ошибок

**1. Несуществующий класс:**
```bash
java -cp bin ReflectionTask Vehicle.Wrong setModelPrice BMW0 5500000
```
```
❌ Ошибка: Класс не найден - Vehicle.Wrong
Убедитесь, что указано правильное полное имя класса
```

**2. Несуществующий метод:**
```bash
java -cp bin ReflectionTask Vehicle.Auto wrongMethod BMW0 5500000
```
```
❌ Ошибка: Метод не найден - wrongMethod
Доступные методы: setModelPrice, getModelPrice, addNewModel...
```

**3. Несуществующая модель:**
```bash
java -cp bin ReflectionTask Vehicle.Auto setModelPrice BMW999 5500000
```
```
❌ Ошибка при выполнении метода: такой модели нет BMW999
```

**4. Цена вне диапазона:**
```bash
java -cp bin ReflectionTask Vehicle.Auto setModelPrice BMW0 500000
```
```
❌ Ошибка при выполнении метода: вы не попали в промежуток цены
```

---

## 📂 Структура проекта

```
lab5/
├── src/
│   ├── Vehicle/
│   │   ├── Vehicle.java           # Интерфейс
│   │   ├── Auto.java              # Класс автомобиля
│   │   ├── Motorcycle.java        # Класс мотоцикла
│   │   └── TransportTools.java
│   ├── Exceptions/
│   │   ├── NoSuchModelNameException.java
│   │   ├── ModelPriceOutOfBoundsException.java
│   │   └── DuplicateModelNameException.java
│   ├── ReflectionTask.java        # ⭐ ОСНОВНОЕ ЗАДАНИЕ
│   ├── ReflectionDemo.java        # ⭐ РАСШИРЕННАЯ ДЕМОНСТРАЦИЯ
│   └── Test.java
├── bin/                            # Скомпилированные классы
├── run.bat                         # Скрипт для компиляции и запуска
├── run_task.bat                    # Скрипт для запуска задачи
├── QUICK_START.md                  # Быстрый старт
├── REFLECTION_INSTRUCTIONS.md      # Подробные инструкции
└── README.md                       # Этот файл
```

---

## 🎓 Демонстрация преподавателю

### Что показать:

1. **✅ Компиляция проекта**
   ```bash
   javac -d bin -encoding UTF-8 src\Vehicle\*.java src\Exceptions\*.java src\ReflectionTask.java
   ```

2. **✅ Запуск с корректными параметрами**
   ```bash
   java -cp bin ReflectionTask Vehicle.Auto setModelPrice BMW0 5500000
   ```

3. **✅ Демонстрация изменения цены**
   - Показать состояние ДО
   - Показать вызов метода через рефлексию
   - Показать состояние ПОСЛЕ

4. **✅ Обработка ошибок**
   - Несуществующий класс
   - Несуществующий метод
   - Несуществующая модель
   - Цена вне диапазона

5. **✅ Код программы**
   - Объяснить использование `Class.forName()`
   - Объяснить создание экземпляра
   - Объяснить получение и вызов метода
   - Показать обработку исключений

6. **✅ Расширенная демонстрация**
   ```bash
   java -cp bin ReflectionDemo
   ```

---

## 💡 Ключевые моменты решения

### 1. Загрузка класса по имени
```java
Class<?> vehicleClass = Class.forName(className);
```
- Динамическая загрузка класса в runtime
- Полное имя класса включает пакет: `Vehicle.Auto`

### 2. Создание экземпляра
```java
Vehicle vehicle = (Vehicle) vehicleClass
    .getConstructor(String.class, int.class)
    .newInstance("TestMark", 3);
```
- Поиск конструктора с нужными параметрами
- Создание объекта через `newInstance()`

### 3. Вызов метода
```java
Method method = vehicleClass.getMethod(methodName, String.class, double.class);
method.invoke(vehicleInstance, modelName, price);
```
- Получение метода по имени и типам параметров
- Вызов метода на экземпляре объекта

### 4. Обработка ошибок
```java
try {
    // Код с рефлексией
} catch (InvocationTargetException e) {
    // Получаем настоящее исключение из метода
    Throwable cause = e.getCause();
}
```

---

## 📖 Дополнительные материалы

### Что такое Reflection?
Рефлексия (Reflection) - механизм Java, позволяющий программе исследовать и модифицировать свою структуру и поведение во время выполнения.

### Применение Reflection:
- ✅ Фреймворки (Spring, Hibernate)
- ✅ Тестирование (JUnit)
- ✅ Сериализация/десериализация
- ✅ Dependency Injection
- ✅ ORM (Object-Relational Mapping)

### Недостатки Reflection:
- ⚠️ Снижение производительности
- ⚠️ Потеря безопасности типов
- ⚠️ Сложность отладки
- ⚠️ Нарушение инкапсуляции

---

## 🔧 Команды для работы

```bash
# Компиляция всего проекта
javac -d bin -encoding UTF-8 src\Vehicle\*.java src\Exceptions\*.java src\*.java

# Запуск основного задания
java -cp bin ReflectionTask Vehicle.Auto setModelPrice BMW0 5500000

# Запуск демонстрации
java -cp bin ReflectionDemo

# Запуск демонстрации с параметрами
java -cp bin ReflectionDemo Vehicle.Auto setModelPrice BMW10 6000000

# Очистка bin
rmdir /s /q bin
mkdir bin
```

---

## ✨ Особенности реализации

1. ✅ **Подробные комментарии** в коде
2. ✅ **Полная обработка ошибок** с понятными сообщениями
3. ✅ **Красивое форматирование** вывода
4. ✅ **Примеры использования** в двух вариантах
5. ✅ **Bat-файлы** для быстрого запуска
6. ✅ **Подробная документация** на русском языке
7. ✅ **Демонстрация всех возможностей** рефлексии

---

## 📞 Поддержка

Если возникли вопросы:
1. Прочитайте `QUICK_START.md` - самый простой способ начать
2. Изучите `REFLECTION_INSTRUCTIONS.md` - подробные инструкции
3. Запустите `ReflectionDemo` - покажет все примеры
4. Проверьте комментарии в коде

---

## 🎉 Заключение

Задание полностью выполнено! Созданы:
- ✅ 2 рабочих программы (`ReflectionTask` и `ReflectionDemo`)
- ✅ Bat-файлы для автоматизации
- ✅ Подробная документация на русском
- ✅ Примеры всех возможных сценариев
- ✅ Обработка всех ошибок

**Удачи на защите лабораторной работы! 🚀**
